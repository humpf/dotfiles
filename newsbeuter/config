## reload at start up
auto-reload yes
## reload every ... minutes
reload-time 30
## sort feeds by first tag in urls
feed-sort-order "firsttag"
## stay in same feed for next unread
#goto-next-feed no
## max number of articles per feed
max-items 50
## selected articles are marked as read
mark-as-read-on-hover yes
## vim keybindings
bind-key j down
bind-key k up
bind-key h left
bind-key l right
bind-key f open
## set browser
browser luakit
